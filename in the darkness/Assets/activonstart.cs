using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class activonstart : MonoBehaviour
{
    public GameObject at;
    // Start is called before the first frame update
    void Start()
    {
        at.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
